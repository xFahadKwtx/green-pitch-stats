# Roadmap

- [x] H2: shared Postgres cache/coordinator for the four public Airtable feeds (code + tests + proposed SQL).
- [x] Clear preview typecheck errors, including the pre-existing ones in the generated backend setup files.
- [ ] Apply the H2 SQL migration (blocked: awaiting review by the calling assistant; must not be applied yet).
